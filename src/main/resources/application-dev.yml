spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/beduno
    username: beduno
    password: beduno
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.beduno: DEBUG
    org.springframework.security: DEBUG
